<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="setExamQuestionDAO">
	
	<insert id="insertSetExamQuestion" parameterType="SetExamQuestionVO">
		<selectKey keyProperty="setExamQuestionId" resultType="String" order="BEFORE">
			SELECT MAX(to_number(SET_EXAM_QUESTION_ID))+1 FROM SET_EXAM_QUESTION
		</selectKey>
		INSERT INTO SET_EXAM_QUESTION
		(	EXAM_ID
			,QUESTION_ID
			,SET_EXAM_QUESTION_ID
			,POINT )
		VALUES(	#{examId}
				,#{questionId}
				,#{setExamQuestionId}
				,#{point} )
	</insert>
	
	<!-- 관리자용 출제 리스트 -->
	<select id="getSetExamQuestionList" resultType="SetExamQuestionVO">
		SELECT 	
			EXAM_ID
			,QUESTION_ID
			,SET_EXAM_QUESTION_ID
			,POINT
		FROM SET_EXAM_QUESTION
		ORDER BY SET_EXAM_QUESTION_ID DESC
	</select>
</mapper>

